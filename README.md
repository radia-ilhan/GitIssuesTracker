# GitIssuesTracker
1. var : function scoped, can be redeclared & updated
Let : block scoped,van update but can’t redeclare in same scope const: block scoped, cannot update or redeclare

2.  for expanding elements of an object

3.map() : creates new array after transforming each element
filter() : creates new array with elements that pass a condition
forEach() : loops through array but returns nothing 

4.shorter way to write functions using =>


5. strings written using backticks for variables easily.
